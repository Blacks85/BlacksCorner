<% if flash.any? %>
	<% flash.each do |key, value| %>
		<div class="grid_8 prefix_2">
			<div id="error-container" class="light-bg round-corners shadow">
				<%= value %>
			</div>
		</div>
		<div class="clear"></div>
	<% end %>
<% end %>